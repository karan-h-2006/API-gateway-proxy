$kind: http-request
name: List API Keys
method: GET
url: '{{baseUrl}}/api/v1/api-keys'
description: List all API keys for the authenticated user. Requires JWT authentication.
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
