$kind: http-request
name: 'Proxy Request (Root)'
method: GET
url: '{{baseUrl}}/proxy'
description: Proxy a request to the configured target URL at the root path. Authenticated via x-api-key header.
headers:
  - key: x-api-key
    value: '{{proxyApiKey}}'
